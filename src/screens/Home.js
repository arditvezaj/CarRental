import { SafeAreaView, View, Text, StyleSheet } from "react-native";
import Card from "../components/Card";

const Home = () => {
  return (
    <SafeAreaView>
      <View>
        <Card />
      </View>
    </SafeAreaView>
  );
};

export default Home;
